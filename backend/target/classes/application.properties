server.port=8080

# ---------- MySQL ----------
spring.datasource.url=jdbc:mysql://localhost:3306/skillsphere_db?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Pocket@11
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---------- JPA / Hibernate ----------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ---------- JWT ----------
# Replace with a long, random secret in production (>= 256 bits for HS256)
jwt.secret=change-this-to-a-long-random-secret-key-min-32-chars-please
jwt.expiration-ms=86400000

# ---------- Google Sign-In ----------
# The OAuth Client ID from Google Cloud Console (Credentials -> OAuth 2.0 Client IDs).
# Must match the client ID used on the frontend (see frontend/.env).
google.client-id=863730013400-717u45eqg4eqbcbufrn99js7lqg9im7c.apps.googleusercontent.com

